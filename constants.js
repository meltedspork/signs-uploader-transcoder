const OUTPUT_BUCKET = process.env.OUTPUT_BUCKET;
const EXTENSION = '.gif';
const MIME_TYPE = 'image/gif';
const THUMB_WIDTH = 300;

module.exports = {
  OUTPUT_BUCKET,
  EXTENSION,
  MIME_TYPE,
  THUMB_WIDTH,
};
